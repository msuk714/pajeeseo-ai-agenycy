import PlaceholderPage from "@/components/PlaceholderPage";

export default function AboutPage() {
  return (
    <PlaceholderPage
      title="About PajeeSEO"
      description="Our story, team, and process — full About page coming in the next build pass."
    />
  );
}
